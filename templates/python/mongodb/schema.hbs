import graphene
from graphene.relay import Node
from graphene_mongo import MongoengineConnectionField, MongoengineObjectType

from models import {{ MappingClass }} as {{ MappingClassModel }}

class {{ MappingClass }}(MongoengineObjectType):
    class Meta:
        model = {{ MappingClassModel}}
        interfaces = (Node, )

class Query(graphene.ObjectType):
    node = Node.Field()
    {{ mappingClass}}= MongoengineConnectionField({{ MappingClass}})

schema = graphene.Schema(query=Query, types=[ {{ MappingClass}} ])